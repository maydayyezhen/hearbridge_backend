<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yezhen.hearbridge.backend.mapper.AdminUserMapper">

    <sql id="BaseColumnList">
        id,
        username,
        password_hash as passwordHash,
        nickname,
        status,
        created_at as createdAt,
        updated_at as updatedAt
    </sql>

    <select id="selectByUsername" resultType="com.yezhen.hearbridge.backend.entity.AdminUser">
        select
        <include refid="BaseColumnList"/>
        from admin_user
        where username = #{username}
        limit 1
    </select>

    <select id="selectById" resultType="com.yezhen.hearbridge.backend.entity.AdminUser">
        select
        <include refid="BaseColumnList"/>
        from admin_user
        where id = #{id}
        limit 1
    </select>

    <update id="updatePassword">
        update admin_user
        set password_hash = #{passwordHash}
        where id = #{id}
    </update>

</mapper>
